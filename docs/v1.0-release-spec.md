# Dupli1 v1.0 release spec & closeout checklist

**Status:** Target **2026-08-30** (was postponed 2026-07-27) — do not tag until every open item in sections A–F below is resolved (no partial ship).  
**Tag when done:** `git tag v1.0` on the commit that passes all **Required** items below **and** has no outstanding launch-blockers in [Known residual risks](#known-residual-risks-accepted-or-fix-before-tag).

**Related:** [v1-release-plan.md](v1-release-plan.md) (narrative plan + [schedule](v1-release-plan.md#schedule)), [v1.1-release-plan.md](v1.1-release-plan.md) (post-launch), [TODO.md](TODO.md) (living backlog), [current-state.md](current-state.md), [deployment-aws.md](deployment-aws.md).

---

## Target date & postponement

**Ship target:** **2026-08-30**. Full product calendar (wallets September, padded October): [v1-release-plan.md § Schedule](v1-release-plan.md#schedule).

v1.0 was put **on hold** on 2026-07-27 until **all** problems in this checklist are resolved — not only backend repo work (section C, which is done) but also production ops (A), prod smoke (B), sibling frontends (D, E), and sign-off (F). That hold remains: the August 30 date is a target, not a waiver of Required rows.

**Do not tag `v1.0`** while any of these remain open:

- Launch-blockers in [Known residual risks](#known-residual-risks-accepted-or-fix-before-tag) (frontend path/pricing/`skuId` alignment)
- Unchecked **Required** rows in sections A, D, or E
- Prod money-path smoke (A10 / B) not passed on `dupli1.com` (or documented staging waiver for F1)
- Backend release gate C5 (`go test ./...` per service on the release commit)

Track open work in [TODO.md](TODO.md); narrative and operator runbook: [v1-release-plan.md](v1-release-plan.md). **v1.1** does not start until v1.0 exit criteria below are met.

---

## What v1.0 is

A **launch cut** for a KRW-only fashion bag marketplace — not feature-complete.

| Property | Spec |
|----------|------|
| Currency | **KRW only** — `*_cents` fields are whole won |
| Customer flow | Browse catalog → PDP → cart → checkout session → order → **pay** (NANO card when configured, else manager **Bypass**) → `paid` → ops ship → `in_transit` → stock committed |
| Manager flow | Catalog/coupon admin, order ship, Bypass payment (`payment.bypass`), Telegram ops alerts |
| Auth | Login → refresh → access JWT (RS256 + JWKS); permissions on access tokens |
| Deploy target | AWS ECS on EC2, ALB HTTPS, RDS PostgreSQL, sibling frontends on same ALB |

### Money path (must work end-to-end)

```text
dupli1-web → gateway → cart (lines + server prices)
                    → order (checkout session → order, server prices)
                    → payment (NANO card when configured; else Bypass; local simulate gated)
                    → NATS payment.succeeded → order paid
                    → manage-web ship → in_transit + inventory commit + soldCount
```

### Explicitly out of v1.0

Guest cart, automated refunds, Bitcoin, co-view recs, manager settings API, email/SMS, chat/analytics, legacy API alias **removal** (aliases may stay until v1.2), zerolog/sessions/access-control hardening (→ **v1.1**). See [v1-release-plan.md](v1-release-plan.md) § D and [v1.1-release-plan.md](v1.1-release-plan.md).

---

## Implementation status (backend `dupli1`)

| Area | Code status | Re-verify on prod deploy |
|------|-------------|---------------------------|
| Server-side order/checkout pricing (**C1**) | Done | Yes — client cannot set charge amount |
| JWT fail-closed (**H7**) | Done | Yes — JWKS always wired in ECS |
| Order idempotency + outbox (**H1**) | Done | Smoke duplicate create |
| Payment outbox + reconcile (**H3**) | Done | Webhook retry safe |
| Cart server prices | Done | — |
| NANO / Bypass + dev simulate gated | Done in code | Card = NANO when `NANO_*` set; else Bypass. Simulate requires `PAYMENT_ALLOW_DEV_SIMULATE`; leave unset on ECS |
| Canonical API paths (+ legacy aliases) | Done | Frontends should prefer canonical |
| Product images CDN **code/Terraform** | Done | Prod `imageUrls` use CloudFront; browsers load without 403 |
| Auth register NATS soft-success | Done | Publish failure is logged; the account survives |
| Notification handler error logging | Done | `notification nats handler subject=… error=…` |
| `api.md` order status `confirmed` | Done | Now `paid` / `in_transit` / `fulfilled` |
| Persistent JWT signing key **code/Terraform** | Done | Prod injects `JWT_PRIVATE_KEY`; `features.ephemeral_jwt_key` is `false` |
| Order migrate on a fresh database | Done | Was broken: the `payment_due_at` index preceded the column |
| Replayed `payment.succeeded` after ship | Done | No longer logs an error every minute for two hours |

---

## Closeout checklist

Use this table to track v1.0 ship. **Required** = blocks tag. **Recommended** = ship soon after or waive in writing.

### A — Production ops (`dupli1` infra / AWS)

| # | Item | Owner | Required | Done |
|---|------|-------|----------|------|
| A1 | Deploy latest **product** to prod; confirm `official_price` migrate ran on RDS (no `selling_price` column) | ops / backend | Yes | [x] |
| A2 | **Product images CDN** — CloudFront + OAC live; `S3_PUBLIC_ENDPOINT` points at CDN — [product-images-browser-access.md](product-images-browser-access.md) | ops | Yes | [x] |
| A3 | Rewrite stale `imageUrls` in DB if hosts still point at private S3 | ops / backend | Yes (if A2 changed host) | [x] |
| A4 | **Card PG** — **NANO** when Secrets Manager `dupli1/production/nano-payment` is wired; otherwise launch money path uses **Bypass** (`payment.bypass`). Historical Stripe adapter removed | ops | NANO optional | [x] |
| A5 | **Bypass** only for managers (`payment.bypass`); `simulate-success` unreachable in prod (`PAYMENT_ALLOW_DEV_SIMULATE` unset on ECS) | ops | Yes | [ ] |
| A6 | **Persistent JWT signing key** — RSA secret `dupli1/production/jwt-private-key` wired into auth as `JWT_PRIVATE_KEY`; `features.ephemeral_jwt_key` is `false` — [deployment-aws.md](deployment-aws.md#jwt-signing-key) | ops | Yes | [x] |
| A7 | **Telegram** — `dupli1/production/telegram` in Secrets Manager wired into notification | ops | Yes (if alerts required) | [x] |
| A8 | Gateway ECS task uses **`api/nginx.ecs.conf`** (CI builds `api/Dockerfile.ecs`; routes healthy) | ops | Yes | [x] |
| A9 | Spot-check catalog **prices** — no widespread `price` / `officialPrice` = 0 after historical wipe | ops / catalog | Yes | [ ] |
| A10 | **Prod smoke** — full money path (below) on `dupli1.com` | ops / QA | Yes | [ ] |

### B — Production smoke script (A10 detail)

Run against production (or staging accepted only if prod not ready — document waiver).
[`scripts/smoke-money-path.sh`](../scripts/smoke-money-path.sh) automates B3–B9 —
`BASE=https://dupli1.com SKU_ID=<existing skuId> scripts/smoke-money-path.sh`.
Without a card PG it pays via **manager Bypass** and asserts simulate is off. B1, B2 and B7 stay manual (browser images and the
Telegram alert).

| Step | Action | Expected |
|------|--------|----------|
| B1 | `GET /gateway/health` | `ok` |
| B2 | Browse active bags; **images load** in browser (not 403) | PDP images visible |
| B3 | Login as customer; add variant to cart | Cart persists |
| B4 | Checkout session → complete → order `pending` | Order created |
| B5 | **Pay** — manager **Bypass** (v1.0 without PG); or card when a PG is later wired | Order can reach `paid` |
| B6 | Order status | `paid` |
| B7 | Telegram (if configured) | Alert on paid order |
| B8 | Manager **ship** order | `in_transit`; stock committed; `soldCount` increments |
| B9 | Customer **Bypass** rejected; manager Bypass succeeds | 403 vs success |

### C — Backend hardening (`dupli1` repo)

| # | Item | Owner | Required | Done |
|---|------|-------|----------|------|
| C1 | Auth **`user.registered`** soft-success — do not delete user if NATS publish fails (open register is on) | backend | Yes if `AUTH_OPEN_REGISTER=true` in prod | [x] |
| C2 | **Notification** — log NATS handler errors (no silent discard) | backend | Recommended | [x] |
| C3 | Fix **`api.md`** order lifecycle — `paid` / `in_transit` / `fulfilled`, not `confirmed` | backend | Recommended | [x] |
| C4 | Refresh **OpenAPI** for inventory, catalog, coupons canonical paths | backend | Recommended | [x] |
| C5 | Re-run **`go test ./...`** per service on release commit | backend | Yes | [ ] |
| C6 | **Persistent JWT key support** — auth reads `JWT_PRIVATE_KEY`; Terraform injects `jwt_private_key_secret_arn` (pairs with A6) | backend | Yes | [x] |
| C7 | **Order migrate works on a fresh database** — `payment_due_at` index created after the column | backend | Yes | [x] |
| C8 | **Replayed `payment.succeeded`** after ship is a no-op, not a per-minute error | backend | Recommended | [x] |

### D — Storefront (`dupli1-web`)

| # | Item | Owner | Required | Done |
|---|------|-------|----------|------|
| D1 | Use **canonical API paths** (`/api/v1/products/…`, `/api/v1/orders/…`, `/api/v1/cart/…`) | web | Yes | [ ] |
| D2 | Parent **`price`** + **`officialPrice`** (not `sellingPrice` / variant price) | web | Yes | [ ] |
| D3 | Cart/checkout lines use **`skuId`** (ULID) where possible; display human `sku` — [frontend-product-variants-migration.md](frontend-product-variants-migration.md) | web | Yes | [ ] |
| D4 | PDP variant picker + parent/variant JSON shape | web | Yes | [ ] |
| D5 | Stock UX — poll inventory or accept oversell until checkout reserve (PDP `inStock` optional) | web | Waivable | [ ] |

### E — Admin (`dupli1-manage-web`)

| # | Item | Owner | Required | Done |
|---|------|-------|----------|------|
| E1 | Canonical API paths + parent pricing (same as D1–D2) | manage-web | Yes | [ ] |
| E2 | **`skuId`** on create/edit variant and order lines where applicable | manage-web | Yes | [ ] |
| E3 | Ship flow → `POST /api/v1/orders/{id}/ship` with manager token | manage-web | Yes | [ ] |
| E4 | Bypass payment UI gated to managers only | manage-web | Yes | [ ] |

### F — Sign-off

| # | Item | Done |
|---|------|------|
| F1 | All **Required** rows in A–E checked (or waived with reason in PR/release notes) | [ ] |
| F2 | [deployment-aws.md](deployment-aws.md) runbook followed for secrets / JWT (NANO optional) | [ ] |
| F3 | [current-state.md](current-state.md) updated — remove stale “client-trusted prices” gap | [ ] |
| F4 | Git tag **`v1.0`** on release commit; GitHub release notes with smoke evidence | [ ] |
| F5 | Open **v1.1** work from [v1.1-release-plan.md](v1.1-release-plan.md) | [ ] |

---

## Exit criteria (summary)

**Target 2026-08-30** — ship **v1.0** only when **all** are true (maps to checklist above; partial completion is not a launch):

- [ ] **A10 / B** — Storefront can browse bags, see images, checkout; pay via **Bypass** (or future PG) → order `paid`
- [ ] **B8** — Ops can ship → `in_transit` and stock commits
- [ ] **A5 / B9** — Bypass manager-only; simulate-success off in prod
- [ ] **A7 / B7** — Telegram fires on `order.paid` (wired; confirm on smoke)
- [ ] **A9** — No known zeroed catalog prices on live products
- [ ] **D + E** — Frontends on canonical paths + parent pricing + `skuId` preferred
- [ ] **F2** — Deployment runbook complete (NANO optional)

---

## Known residual risks (accepted or fix before tag)

| Risk | Severity | Checklist / action |
|------|----------|-------------------|
| Images 403 without CDN | Fixed | A2, A3, B2 — prod `imageUrls` already use CloudFront |
| Frontend legacy paths / `sku` only | **Launch-blocker** | D, E |
| Zeroed catalog prices | **Data** | A9 |
| Ephemeral JWT key in prod — every deploy logs all users out | Fixed | A6 — secret live; `ephemeral_jwt_key: false` |
| Register rolls back on NATS flap | Fixed | C1 |
| `api.md` stale `confirmed` status | Fixed | C3 |
| Admin/owner never locked out | Accepted | Rate limits + strong passwords |
| Legacy API aliases still in nginx | Accepted until v1.2 | Document in release notes |

---

## After v1.0

1. Tag `v1.0` and publish short release notes (smoke date, commit SHA, known waivers).  
2. Start [v1.1-release-plan.md](v1.1-release-plan.md) — logging (zerolog), sessions, access control, deployment, automation.  
3. **September 2026** — add **wallet** products ([product-multi-category-design.md](product-multi-category-design.md) phases 1–2).  
4. **October 2026** — add **padded** bag-line products to the catalog.  
5. Track remaining commerce backlog under **v1.2** in [TODO.md](TODO.md).

---

## Doc maintenance

- **This file** is the v1.0 **spec + closeout checklist** (checkboxes live here).
- [v1-release-plan.md](v1-release-plan.md) stays the narrative boundary and defer list.
- [TODO.md](TODO.md) — link v1.0 open items here; do not duplicate long checklists in both places.
